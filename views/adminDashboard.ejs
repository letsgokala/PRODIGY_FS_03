<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/admin">Admin Dashboard</a>
            <div class="d-flex">
                <a href="/profile" class="btn btn-outline-light me-2">Profile</a>
                <a href="/logout" class="btn btn-danger">Logout</a>
            </div>
        </div>
    </nav>
    <div class="container mt-4">
        <h2>Admin Dashboard</h2>
        <div class="row">
            <div class="col-md-4">
                <div class="card p-3 bg-info text-white">
                    <h5>Total Orders</h5>
                    <p class="fs-3"><%= totalOrders %></p>
                </div>
            </div>
            <div class="col-md-4">
                <div class="card p-3 bg-warning text-white">
                    <h5>Pending Orders</h5>
                    <p class="fs-3"><%= pendingOrders %></p>
                </div>
            </div>
            <div class="col-md-4">
                <div class="card p-3 bg-success text-white">
                    <h5>Products You Added</h5>
                    <p class="fs-3"><%= adminProducts %></p>
                </div>
            </div>
        </div>
        <a href="/admin/products" class="btn btn-primary mt-3">Manage Products</a>
        <a href="/admin/orders" class="btn btn-secondary mt-3">Manage Orders</a>
    </div>
</body>
</html>
